from setuptools import setup, find_packages

setup(
    name='myport',
    version='1.0',
    packages=find_packages(),
    install_requires=[
        'Django==4.2',  # Substitua com a versão específica que seu projeto requer
        # Outras dependências aqui
    ],
    # Outras configurações aqui
)
